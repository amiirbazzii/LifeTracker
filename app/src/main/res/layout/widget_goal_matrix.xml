<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/matrix_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg_black"
    android:orientation="vertical"
    android:paddingLeft="14dp"
    android:paddingTop="12dp"
    android:paddingRight="14dp"
    android:paddingBottom="12dp">

    <!-- Header Text: MACRO TIMELINE MATRIX (540 WEEKS) -->
    <TextView
        android:id="@+id/matrix_widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:singleLine="true"
        android:text="MACRO TIMELINE MATRIX (540 WEEKS)"
        android:textColor="#A1A1AA"
        android:textSize="11sp"
        android:textStyle="bold"
        android:typeface="monospace" />

    <!-- Scrollable ListView containing only the matrix rows of squares -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/matrix_widget_row_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@android:color/transparent"
            android:dividerHeight="0dp"
            android:scrollbars="none" />

        <!-- Empty State -->
        <LinearLayout
            android:id="@+id/matrix_widget_empty_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="TIMELINE NOT SET"
                android:textColor="#71717A"
                android:textSize="11sp"
                android:textStyle="bold"
                android:typeface="monospace" />
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
